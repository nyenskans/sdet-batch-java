package com.syntax.class026;

public class ArrayListDemo4 {
    public static void main(String[] args) {
    // Primitive data type // non primitive class for that primitive data type

        byte b=10;
        Byte b1=11;

        short s=20;
        Short s1=21;

        char c='m';
        Character c1='n';

        boolean isCorrect=true;
        Boolean isIncorrect=false;

        int num=30;
        Integer num1=31;

        float f=40; // 40f is optional--> typecasting (conversion) is done automatically by java (widening)
        Float f1=41f; // but the automatic conversion is not done for classes (non primitive data types), so we have to write it ourselves

        double d=50; // 50d is optional--> typecasting (conversion) is done automatically by java (widening)
        Double d1=51d; // but the automatic conversion is not done for classes, so we have to write it ourselves

        long l=60; // 60l is optional--> typecasting (conversion) is done automatically by java (widening)
        Long l1=61l; // but the automatic conversion is not done for classes, so we have to write it ourselves

    }
}
